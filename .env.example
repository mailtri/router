# Environment Configuration for mailtri-router
# Copy this file to .env and adjust values for your environment

# =============================================================================
# ENVIRONMENT MODE
# =============================================================================
# Set to 'true' for local development, 'false' or unset for production
IS_LOCAL=true

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Port for local development server (only used when IS_LOCAL=true)
PORT=3030

# =============================================================================
# AWS CONFIGURATION
# =============================================================================
# AWS Region (defaults to us-east-1)
AWS_REGION=us-east-1

# AWS Endpoint URL (only needed for local development with LocalStack)
# Leave unset for production to use default AWS endpoints
AWS_ENDPOINT_URL=http://localhost:4566

# AWS Credentials (only needed for local development)
# Leave unset for production to use IAM roles/instance profiles
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

# =============================================================================
# AWS RESOURCE CONFIGURATION
# =============================================================================
# S3 Bucket for storing emails
# Local: mailtri-emails
# Production: mailtri-emails-{account}-{region} (set by CDK)
S3_BUCKET=mailtri-emails

# SQS Queue Name for processed emails
# Local: mailtri-processed-emails
# Production: mailtri-processed-emails (set by CDK)
SQS_QUEUE_NAME=mailtri-processed-emails

# SQS Queue URL (optional - will be constructed from queue name if not provided)
# Local: http://localhost:4566/000000000000/mailtri-processed-emails
# Production: https://sqs.{region}.amazonaws.com/{account}/mailtri-processed-emails
# SQS_QUEUE_URL=

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================
# Webhook URL for sending processed email notifications (optional)
# WEBHOOK_URL=https://your-webhook-endpoint.com/email-processed

# =============================================================================
# LOCAL DEVELOPMENT SETUP
# =============================================================================
# For local development, you typically only need:
# IS_LOCAL=true
# AWS_ENDPOINT_URL=http://localhost:4566
# AWS_ACCESS_KEY_ID=test
# AWS_SECRET_ACCESS_KEY=test
# S3_BUCKET=mailtri-emails
# SQS_QUEUE_NAME=mailtri-processed-emails

# =============================================================================
# PRODUCTION SETUP
# =============================================================================
# For production, you typically only need:
# IS_LOCAL=false (or unset)
# S3_BUCKET=mailtri-emails-{account}-{region}
# SQS_QUEUE_NAME=mailtri-processed-emails
# WEBHOOK_URL=https://your-webhook-endpoint.com/email-processed (optional)
